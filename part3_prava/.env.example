# Part 3 — CLI agent for discover/quote (production shop).
# One-time:
#   npm run setup && npm run setup:poll && npm run status
#
# Sandbox pay (CARD-03) — required for team test card:
#   CLI/MCP cannot do sandbox (Prava: production only).
#   Get sk_test_ from https://dashboard.prava.space
PRAVA_SECRET_KEY=sk_test_REPLACE_ME
# PRAVA_SANDBOX_API_BASE=https://sandbox.api.prava.space
# PRAVA_SANDBOX_USER_ID=milkwatch_sevenarc
# PRAVA_SANDBOX_USER_EMAIL=you@example.com
# PRAVA_PAY_MODE=sandbox
# SANDBOX_TRY_CLI_CHECKOUT=0
#
# CARD-03 (sandbox only): 4622943123232200 / CVV 93 / exp 12/30 / OTP 456789
#
# API: npm run start  →  http://127.0.0.1:8788
# PORT=8788
# UI_PORT=8790
# CORS_ORIGINS=http://127.0.0.1:8765,http://localhost:8765
